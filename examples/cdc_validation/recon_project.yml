name: cdc_validation_example
version: 0.1.0
config-version: 1

profile: dev

contract-paths:
  - contracts

sample-policy-paths:
  - sample_policies

tolerance-policy-paths:
  - tolerances

schema-policy-paths:
  - schema_policies

target-path: target
report-path: reports

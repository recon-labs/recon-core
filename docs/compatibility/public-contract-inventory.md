# Public Contract Inventory

## Purpose

This inventory lists the Recon surfaces that users, adapters, packages,
integrations, generated-artifact readers, CI workflows, or future repositories
may depend on.

Treat these surfaces as public contract surfaces even before 1.0. Pre-alpha
surfaces can still change, but changes must be deliberate, documented, and
reviewed for compatibility impact.

## Current inventory

| Public contract surface | Current status | Primary docs | Code version constant |
| --- | --- | --- | --- |
| Contract YAML schema | Parser scope implemented; not frozen before 1.0. | `docs/framework/equivalence-contracts.md`, `docs/user-guide/equivalence-contracts.md` | Not centralized yet. |
| Contract schema stabilization and deprecation policy | Planned; no 1.0 schema freeze, deprecation lifecycle, or migration-warning behavior exists yet. | `docs/planning/release-plan.md`, `docs/implementation/mvp-build-order.md` | Add schema/deprecation versioning only when implemented behavior exists. |
| Named identities and multi-grain contracts | Planned; current contracts support one default `grain.keys` and one default `cdc.keys`. | `docs/decisions/adr-0014-key-semantics-and-check-dependencies.md`, `docs/implementation/mvp-build-order.md` | Covered by contract YAML, compiled artifact, run result, and evidence versions when implemented. |
| Manifest artifact schema | Implemented for `recon parse`. | `docs/implementation/parser-and-manifest.md`, `docs/compatibility/artifact-versions.md` | `MANIFEST_ARTIFACT_VERSION = 1` |
| Compiled artifact schema | Implemented for compiled contract and compiled checks YAML. | `docs/decisions/adr-0015-compiled-artifact-schema-and-versioning.md`, `docs/implementation/compiled-artifacts.md` | `COMPILED_ARTIFACT_VERSION = 1` |
| Compiled SQL artifacts | Implemented for `recon compile --render-sql` with current DuckDB relation-backed typed plans. | `docs/decisions/adr-0020-milestone-6-adapter-profile-and-sql-rendering-boundary.md`, `docs/implementation/compiled-artifacts.md`, `docs/compatibility/artifact-versions.md` | No independent version constant; SQL paths and `rendering.adapter_type` are referenced from compiled checks artifact version 1. |
| Generated artifact lifecycle and cleanup | Implemented for current manifest, compiled YAML, and compiled SQL outputs; current compile cleanup rejects unsafe paths and avoids stale, partial, or orphaned generated outputs. Future run results, evidence, failure details, reports, state, docs output, and selector-scoped artifacts must define cleanup and publish ordering before writing generated files. | `docs/architecture/artifact-model.md`, `docs/compatibility/artifact-versions.md`, `docs/implementation/compiled-artifacts.md`, `docs/implementation/mvp-build-order.md` | Covered by each concrete artifact version when implemented. |
| Artifact freshness and cache semantics | Planned; cache optimization and skip-unchanged behavior are not implemented yet. | `docs/architecture/parse-compile-run.md`, `docs/compatibility/artifact-versions.md`, `docs/implementation/mvp-build-order.md` | Add artifact/cache version fields only when emitted behavior exists. |
| Diagnostic codes and validation timing | Implemented for current parse/compile scope; Milestone 5 timing and code ownership locked. | `docs/decisions/adr-0016-validation-timing-and-diagnostic-codes.md`, `docs/implementation/errors-and-diagnostics.md`, `docs/architecture/diagnostics-and-errors.md` | No separate version constant. |
| Diagnostic output rendering | Current CLI failures print diagnostic code and safe message. Future manifest, compiled artifact, run result, evidence/report, debug/profile command, adapter, and adapter test-kit diagnostic views must preserve safe actionable messages instead of relying on codes or hints alone. | `docs/architecture/diagnostics-and-errors.md`, `docs/implementation/errors-and-diagnostics.md`, `docs/implementation/cli-services.md`, `docs/user-guide/cli.md` | Covered by the artifact or API version for each concrete output surface when implemented. |
| Low-level exception diagnostic sanitization | Current project-config and resource YAML diagnostics suppress raw parser snippets, and current adapter-aware compile suppresses unsafe profile-backed adapter diagnostic text. Future parser, config, adapter, database, runtime, evidence, debug, and test-kit surfaces must summarize low-level exceptions before public output when they can contain authored YAML snippets, rendered profile values, credentials, source/target query text, relation names, row values, or database error payloads. | `docs/architecture/diagnostics-and-errors.md`, `docs/implementation/errors-and-diagnostics.md`, `docs/implementation/testing-plan.md`, `docs/compatibility/change-checklist.md` | Covered by each diagnostic-producing artifact, command, API, or test-kit surface when implemented. |
| Project resource loading and reference resolution | Contract parsing implemented; local non-contract source-file indexing implemented for manifest file metadata; typed non-contract resource loading, package precedence, and reference resolution are not implemented. | `docs/decisions/adr-0017-project-resource-loading-and-precedence.md`, `docs/compatibility/resource-loading.md`, `docs/architecture/project-loading-and-config.md` | No separate version constant. |
| Check-pack invocation config | Current compiler supports strings and `{name}` mappings only; future `config` and `on_empty` shape locked, not implemented. | `docs/decisions/adr-0018-check-pack-invocation-config.md`, `docs/compatibility/check-pack-invocation.md`, `docs/framework/check-packs.md` | Covered by contract YAML and compiled artifact versions when implemented. |
| Local custom check-pack resource schema | Planned; local check-pack files may be discovered/indexed before they become executable resources. | `docs/framework/check-packs.md`, `docs/decisions/adr-0017-project-resource-loading-and-precedence.md`, `docs/implementation/mvp-build-order.md` | Covered by resource schema, contract YAML, compiled artifact, run result, and evidence versions when implemented. |
| Local reusable policy resource schemas | Planned; local sampling, tolerance, and schema policy files are not resolved as named resources yet. | `docs/framework/sampling-policies.md`, `docs/framework/tolerance-policies.md`, `docs/framework/schema-policies.md`, `docs/implementation/mvp-build-order.md` | Covered by resource schema, contract YAML, typed check-plan, result, evidence, and state versions when implemented. |
| Column and value comparison surface | Raw authored columns are preserved; current typed column declaration/reference validation is implemented; value checks, all-column expansion, resolved column metadata, eligibility enforcement, and adapter metadata validation are not implemented. | `docs/decisions/adr-0019-column-and-value-comparison-surface.md`, `docs/compatibility/column-value-comparison.md`, `docs/framework/equivalence-contracts.md` | Covered by contract YAML, compiled artifact, and typed check-plan versions when implemented. |
| Tolerance, null, and normalization policy | High-level authored fields exist; MVP policy surface locked, full resolver and execution not implemented. | `docs/decisions/adr-0009-tolerance-normalization-and-null-equivalence.md`, `docs/compatibility/tolerance-null-normalization.md`, `docs/framework/tolerance-policies.md` | Covered by contract YAML, compiled artifact, typed check-plan, run result, and evidence versions when implemented. |
| Endpoint resources and query execution | Inline endpoint shape exists; endpoint resources, endpoint refs, and executable query endpoints are planned and gated. Milestone 6 adapter-aware behavior is relation-only. | `docs/framework/equivalence-contracts.md`, `docs/architecture/project-loading-and-config.md`, `docs/implementation/mvp-build-order.md`, `docs/decisions/adr-0020-milestone-6-adapter-profile-and-sql-rendering-boundary.md` | Covered by contract YAML, adapter API, compiled artifact, run result, and evidence versions when implemented. |
| Selector and subset execution | Planned; `selectors.yml`, `--select`, `--exclude`, partial compile, and partial run semantics are not locked yet. | `docs/framework/project-structure.md`, `docs/user-guide/cli.md`, `docs/implementation/cli-services.md`, `docs/implementation/mvp-build-order.md` | Covered by CLI behavior, manifest, compiled artifact, and run result versions when implemented. |
| Sampling execution and stateful policies | Authored sampling fields exist at a high level; deterministic execution, anchor-side semantics, persisted samples, previous-failure sampling, and multi-policy composition are gated. | `docs/framework/sampling-policies.md`, `docs/framework/state-and-watermarks.md`, `docs/implementation/sampling-engine.md`, `docs/implementation/mvp-build-order.md` | Covered by contract YAML, typed check-plan, run result, evidence, and state versions when implemented. |
| CDC policy and delete semantics | High-level CDC docs and key semantics exist; first CDC execution, asymmetric delete representation, and advanced CDC modes are gated. | `docs/decisions/adr-0011-cdc-policy-and-delete-modes.md`, `docs/decisions/adr-0014-key-semantics-and-check-dependencies.md`, `docs/implementation/cdc-policy-engine.md`, `docs/implementation/mvp-build-order.md` | Covered by contract YAML, compiled artifact, run result, evidence, and state versions when implemented. |
| Semi-structured comparison | Draft adapter capabilities mention JSON/semi-structured support; public syntax and execution semantics are not implemented. | `docs/compatibility/capability-catalog.md`, `docs/implementation/mvp-build-order.md` | Covered by contract YAML, typed check-plan, adapter API, run result, and evidence versions when implemented. |
| Typed check plan schema | Draft typed plans are produced in compiled checks artifacts; Milestone 6 renders current operations only and does not expand the operation catalog. | `docs/decisions/adr-0013-typed-check-plans-and-adapter-sql-rendering.md`, `docs/decisions/adr-0020-milestone-6-adapter-profile-and-sql-rendering-boundary.md`, `docs/compatibility/typed-check-plan.md` | Add `TYPED_CHECK_PLAN_VERSION` only if typed plans get an independent version. |
| Profile and secret handling | Implemented for adapter-aware compile; selected-profile/target loading, referenced-connection filtering, env-var rendering, unsupported-template rejection, same-context render-sql enforcement, profile-backed adapter diagnostic suppression including adapter API compatibility and render-phase diagnostics, sanitized adapter factory exceptions, sanitized adapter metadata exceptions, sanitized capability declaration exceptions, and `rendering.adapter_type` redaction exist. Case-variant and non-string rendered-config redaction is implemented for current profile-backed adapter diagnostics, including unsafe `resource_type`, `resource_name`, `line`, and `column` values and short numeric rendered scalars such as port values in diagnostic text, unsafe resource metadata, and `rendering.adapter_type`. Runtime profile loading, debug/profile validation commands, and shared adapter test-kit conformance are future work and must keep these redaction guarantees. | `docs/decisions/adr-0020-milestone-6-adapter-profile-and-sql-rendering-boundary.md`, `docs/architecture/project-loading-and-config.md`, `docs/implementation/cli-services.md`, `docs/compatibility/adapter-api.md` | Add code constants only if a profile schema version is implemented. |
| Source/target data privacy and public output | Planned and gated before execution/results/evidence surfaces expose source or target data; current malformed YAML diagnostics already suppress raw parser snippets because authored contracts can contain source/target query text. Privacy defaults must cover terminal output, logs, diagnostics, run results, evidence, reports, failure details, result tables, adapter runtime errors, and adapter test-kit snapshots. Raw source/target rows, comparison keys, normalized values, aggregate values, row counts, relation names, query text, and runtime error text must be classified as public, sensitive, or policy-controlled before implementation. | `docs/framework/evidence.md`, `docs/user-guide/evidence.md`, `docs/implementation/evidence-writers.md`, `docs/implementation/result-model.md`, `docs/implementation/errors-and-diagnostics.md`, `docs/implementation/mvp-build-order.md` | Covered by run result, evidence, failure detail, and result table versions when implemented. |
| Adapter API | Implemented as a pre-alpha API boundary; not stable for external adapter packages yet. | `docs/architecture/adapter-interface.md`, `docs/implementation/adapter-interface-spec.md`, `docs/compatibility/adapter-api.md` | `ADAPTER_API_VERSION = "1"` |
| Capability catalog | Draft catalog documented and represented by compiler enums; support-state validation is implemented and the in-core DuckDB local adapter declares the current rendering subset. | `docs/compatibility/capability-catalog.md`, `docs/framework/adapters.md` | `AdapterCapability` enum |
| Comparison execution placement | Planned; must be resolved before check-engine execution. | `docs/architecture/check-engine.md`, `docs/decisions/adr-0020-milestone-6-adapter-profile-and-sql-rendering-boundary.md`, `docs/implementation/mvp-build-order.md` | Covered by typed check-plan, adapter API, run result, evidence, and state versions when implemented. |
| Adapter install extras and packaging strategy | `recon-core[duckdb]` is implemented for the in-core DuckDB local development adapter; external adapter packages remain planned. | `docs/planning/release-plan.md`, `docs/planning/ecosystem-roadmap.md`, `docs/implementation/mvp-build-order.md` | Package metadata exposes the current `duckdb` optional extra. |
| Adapter test kit | Planned; no shared adapter compliance test kit exists yet. Before it is created or split into its own repo, it must include adapter API conformance for empty or malformed factory results, malformed factory diagnostic payloads, factories that return both adapters and diagnostics, missing or invalid adapter API version declarations, invalid or exception-raising `adapter_type` metadata, malformed capability support states, sanitized factory exceptions, sanitized adapter metadata exceptions, sanitized capability declaration exceptions, adapter API compatibility diagnostics, render-phase diagnostics, adapter setup failures that write no compiled SQL, mark affected compiled checks blocked, de-duplicate repeated same-connection service diagnostics, and preserve distinct source/target connection diagnostics in service output and blocked compiled-check artifacts, empty and malformed renderer output failures, profile-rendering behavior, field-by-field diagnostic and `rendering.adapter_type` redaction including numeric `line`/`column` fields and short numeric rendered scalars such as `port: 12` in diagnostic text, unsafe resource metadata, and `rendering.adapter_type`, and core render-sql compile-validation blocked-metadata integration cases when the test kit drives core compile flows. | `docs/compatibility/adapter-api.md`, `docs/compatibility/capability-catalog.md`, `docs/compatibility/compatibility-matrix.md`, `docs/implementation/mvp-build-order.md` | Add test-kit compatibility/version docs when the test kit exists. |
| CLI command and option behavior | MVP commands are pre-alpha; future commands/options, documentation generation, and destructive init overwrite behavior are gated before automation can rely on them. | `docs/user-guide/cli.md`, `docs/architecture/cli-architecture.md`, `docs/implementation/cli-services.md`, `docs/implementation/mvp-build-order.md` | No separate version constant. |
| Run result schema | Planned; not implemented yet; source/target value privacy defaults and diagnostic sanitization must be locked before `target/run_results.json` is implemented. | `docs/implementation/result-model.md`, `docs/framework/evidence.md`, `docs/implementation/errors-and-diagnostics.md` | Add `RUN_RESULT_VERSION` when run result artifacts are implemented. |
| Evidence and report schema | Planned; not implemented yet; source/target value privacy defaults, failure-detail policy, truncation behavior, and redaction/masking defaults must be locked before evidence/report output is implemented. | `docs/framework/evidence.md`, `docs/user-guide/evidence.md`, `docs/implementation/evidence-writers.md` | Add version constants when evidence artifacts have stable machine-readable formats. |
| Result table writer schema | Planned; no database/table result writer exists yet. | `docs/framework/evidence.md`, `docs/implementation/evidence-writers.md`, `docs/implementation/result-model.md`, `docs/implementation/mvp-build-order.md` | Add table schema/version docs when implemented. |
| Failure detail schema | Planned; not implemented yet; raw-value export defaults, masking/redaction behavior, JSONL, streaming, and large-result handling are future gated. | `docs/architecture/artifact-model.md`, `docs/implementation/result-model.md`, `docs/implementation/evidence-writers.md` | Add a version constant only if failure details become machine-readable artifacts. |
| State, watermark, and sample-key formats | Planned; local state is future gated and remote/database state is separately gated. | `docs/framework/state-and-watermarks.md`, `docs/implementation/sampling-engine.md`, `docs/implementation/mvp-build-order.md` | Add version constants when state artifacts or remote state schemas are implemented. |
| Package resource schema | Planned; namespace and precedence design locked, but local/package resource schema execution and official package content releases are not implemented yet. | `docs/decisions/adr-0017-project-resource-loading-and-precedence.md`, `docs/framework/packages.md`, `docs/framework/hub.md`, `docs/compatibility/resource-loading.md`, `docs/implementation/mvp-build-order.md` | Add package schema/version constants when package loading is implemented. |
| Package dependency installer and lock workflow | Planned; `recon deps`, `packages.yml`, package locks, and install/update behavior are not implemented yet. | `docs/framework/packages.md`, `docs/framework/hub.md`, `docs/planning/ecosystem-roadmap.md`, `docs/implementation/mvp-build-order.md` | Add package lock/schema version docs when implemented. |
| Official package content and domain packages | Planned; no official package contents, domain-package support policy, or domain package compatibility ranges exist yet. | `docs/planning/ecosystem-roadmap.md`, `docs/product/reconciliation-taxonomy.md`, `docs/implementation/mvp-build-order.md` | Covered by package schema/version docs and compatibility matrix when implemented. |
| Hub and integration metadata | Planned; Hub, GitHub Actions, orchestrator integrations, data catalog integrations, and issue integrations have no stable metadata contract yet. | `docs/framework/hub.md`, `docs/planning/ecosystem-roadmap.md`, `docs/planning/roadmap.md`, `docs/implementation/mvp-build-order.md` | Add metadata/schema versioning when Hub or integration manifests exist. |
| Documentation and example repository compatibility | Planned; docs and examples live in `recon-core` until split criteria are met. | `docs/planning/ecosystem-roadmap.md`, `docs/implementation/mvp-build-order.md` | Not a code constant. |
| Hosted service, UI, and enterprise integration boundaries | Planned only if product direction expands beyond the open-source core. | `docs/product/scope-and-non-goals.md`, `docs/planning/roadmap.md`, `docs/implementation/mvp-build-order.md` | Add integration metadata/versioning only if such products exist. |
| Diagnostic source locations | Path-level diagnostics exist; line, column, span, and range output is planned and gated. | `docs/decisions/adr-0016-validation-timing-and-diagnostic-codes.md`, `docs/implementation/errors-and-diagnostics.md`, `docs/implementation/mvp-build-order.md` | Covered by artifact schemas that carry diagnostics when implemented. |
| Cross-repo compatibility matrix | Documented as current/future matrix. | `docs/compatibility/compatibility-matrix.md` | Not a code constant. |

## Version constant policy

Code version constants should exist only for surfaces that code can actually
produce, consume, validate, or reject.

Use this policy:

- keep `MANIFEST_ARTIFACT_VERSION` because `target/manifest.json` is implemented,
- keep `COMPILED_ARTIFACT_VERSION` because compiled artifact writers are
  implemented,
- add `TYPED_CHECK_PLAN_VERSION` only if typed check plans become independently
  versioned from compiled artifact schemas,
- keep `ADAPTER_API_VERSION` because the adapter API exists in code,
- add `RUN_RESULT_VERSION` when `target/run_results.json` is implemented,
- avoid placeholder constants that imply a stable API before the surface exists.

## Public contract change rule

A change touches a public contract surface when it changes:

- accepted YAML syntax,
- validation defaults or error behavior users rely on,
- generated artifact fields, paths, IDs, versions, or semantics,
- artifact freshness, cache, or stale-artifact reuse semantics,
- typed check-plan operation names, payloads, requirements, or rendering states,
- adapter interfaces, capabilities, registry behavior, or version support,
- profile selection, environment rendering, or secret redaction behavior,
- diagnostic code, message, redaction, or rendering behavior that users,
  artifacts, automation, adapters, or test kits rely on,
- comparison execution-placement or fallback behavior,
- result, evidence, failure detail, state, or watermark formats,
- package resource schema, package lock, package installer, or package
  compatibility behavior,
- support ranges for Python, `recon-core`, adapters, packages, or test kits,
- deprecation, migration, or cross-repo compatibility promises.

When a public contract surface changes, use
`docs/compatibility/change-checklist.md`.

If a new public surface appears later, add it to this inventory in the same
change that introduces the surface.

## Related docs

- `docs/compatibility/change-checklist.md`
- `docs/compatibility/artifact-versions.md`
- `docs/compatibility/adapter-api.md`
- `docs/compatibility/typed-check-plan.md`
- `docs/compatibility/capability-catalog.md`
- `docs/compatibility/compatibility-matrix.md`
